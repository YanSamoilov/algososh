import { recursion } from "../../utils/string";

describe("Проверка разворота строки", () => {
  it("С чётным количеством символов", () => {
    expect(recursion([1, 2, 3, 4], () => {}))
  });
})
[4, 3, 2, 1]
